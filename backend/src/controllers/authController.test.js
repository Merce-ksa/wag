const User = require('../models/userModel');
const authController = require('./folderController');

jest.mock('../models/userModel');

describe('', () => {
  describe('', () => {
    test('', () => {
      expect().toHaveBeenCalled();
    });
  });
});
